The 2-matrix_divided module
==========================

Using matrix_divided
---------------------

This is matrix_divided text file in reStructuredText format.  First import
matrix_divided from the 2-matrix_divided module:

     >>> matrix_divided = __import__("2-matrix_divided").matrix_divided

Now use it:

    divide the matrix provided by div.
    if div is float
    >>> matrix_divided([[1, 2, 3], [4, 5, 6]], 3.0)
    [[0.33, 0.67, 1.0], [1.33, 1.67, 2.0]]

    if div is int
    >>> matrix_divided([[1, 2, 3], [4, 5, 6]], 3)
    [[0.33, 0.67, 1.0], [1.33, 1.67, 2.0]]

    Exceptions
    div test:
    if div is not int and not float
    >>> matrix_divided([[1, 2, 3], [4, 5, 6]], "a")
    Traceback (most recent call last):
        ...
    TypeError: div must be a number

    if div is not int and not float
    >>> matrix_divided([[1, 2, 3], [4, 5, 6]], [1])
    Traceback (most recent call last):
        ...
    TypeError: div must be a number

    if div is zero
    >>> matrix_divided([[1, 2, 3], [4, 5, 6]], 0)
    Traceback (most recent call last):
        ...
    ZeroDivisionError: division by zero

    test for matrix

    if matrix is not list:
    >>> matrix_divided("welcome", 3)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats

    if length of matrix is zero:
    >>> matrix_divided([], 3)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats

    if first inner matrix is tuple and not list
    >>> matrix_divided([("welcome"), ("here")], 3)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats

    if length of first inner matrix is zero
    >>> matrix_divided([[], []], 3)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats

    if matrix inner value is not int or float
    >>> matrix_divided([["welcome"], ["here"]], 3)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats
